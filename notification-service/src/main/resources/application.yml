spring:
  application:
    name: notification-service
  rabbitmq:
    host: localhost

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    ip-address: 10.163.14.67

server:
  port: 8094 # Assign a random port
